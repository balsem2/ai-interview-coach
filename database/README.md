# Dataset

Place the interview questions dataset here before importing data with Docker.

Expected filename:

```text
Mock_interview_questions.json
```

This folder is mounted inside the backend container at:

```text
/app/data
```
