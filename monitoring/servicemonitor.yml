apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-interview-backend
  namespace: monitoring
  labels:
    release: monitoring
spec:
  namespaceSelector:
    matchNames:
      - ai-interview
  selector:
    matchLabels:
      app: backend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
